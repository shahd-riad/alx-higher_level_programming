#!/usr/bin/python3
def multiple_returns(sentence):
    length = len(sentence)
    if sentence == 0:
        firstL = 'None'
        return lenght, firstL
    else:
        firstL = sentence[0]
        return length, firstL
